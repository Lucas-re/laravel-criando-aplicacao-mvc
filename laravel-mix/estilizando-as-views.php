<?php
/**
 * Vamos começar estilizando a pagina que lista as series: 
 * na tag ul adicionamos a classe class="list-group" e na tag li adicionamos a classe class="list-group-item
 * 
 * Vamos adicionar todo o conteudo da lista dentro de um container. O container vamos criar no component layout.
 * Nele adicionamos uma div dentro da tag body com a classe container class="container", e todo o conteudo fica
 * adicionado dentro dessa div
 * 
 * Agora vamos transformar o link em um botão. 
 * Nele adicionamos a classe class="btn btn-dark"
 * 
 * 
 * Agora vamos estilizar a tela do formulario. 
 * Vamos transformar o formulario em um form group. Para isso criamos uma tag div, envolvendo os elementos do formulario.
 * Na tag label adicionamos a classe class="form-label
 * Na tag input adicionamos a classe class="form-control
 * 
 */